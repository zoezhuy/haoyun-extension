# Local development only. Never commit a real API key.
OPENAI_API_KEY=
# Optional during unpacked development. Set exact comma-separated
# chrome-extension:// origins once the extension ID is stable.
ALLOWED_EXTENSION_ORIGINS=
BACKEND_PORT=8787
